/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CommentPresentationComponent } from './comment-presentation.component';

describe('CommentPresentationComponent', () => {
  let component: CommentPresentationComponent;
  let fixture: ComponentFixture<CommentPresentationComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CommentPresentationComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(CommentPresentationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/

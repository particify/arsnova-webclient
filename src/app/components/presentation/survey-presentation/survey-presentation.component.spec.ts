/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SurveyPresentationComponent } from './survey-presentation.component';

describe('SurveyPresentationComponent', () => {
  let component: SurveyPresentationComponent;
  let fixture: ComponentFixture<SurveyPresentationComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ SurveyPresentationComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(SurveyPresentationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
